// weft-services: engine-side services for the RFC-0002 workstreams (WS-1
// activation scheduling first). Pure Java 21 — no Minecraft imports
// (enforced by root verifyNoMinecraftImports); the NeoForge module supplies
// entity positions and applies the decisions via mixin hooks.
